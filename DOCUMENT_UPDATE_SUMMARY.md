# 数据转换指南增强总结

## 更新日期
2026年3月15日

## 更新内容

### 原始需求
- **Request**: 增加原始数据集和转换后数据集的具体样本对比
- **特别强调**: 详细介绍目标点坐标数据的生成方式，并举例说明两种格式数据的区别

### 完成的工作

#### 1. 新增附录A: 原始R2R数据样本

**内容覆盖:**
- A.1: 场景与Episode基本信息
  - 原始目录结构图
  - LeRobot格式的文件组织方式
  
- A.2: 原始数据样本 (episodes.jsonl)
  - 样本1: 简单任务 (46帧, 简短指令)
  - 样本2: 复杂多步任务 (54帧, 长指令)
  - 样本3: 最短任务 (28帧, 简短指令)
  - 每个样本都有详细的字段说明和特点分析

- A.3: 低维Parquet数据样本
  - Parquet数据列结构的表格展示
  - **详细讲解动作列** (0=STOP, 1=↑FORWARD等)
  - **详细讲解位姿列** (4×4变换矩阵表示)
  - **重点讲解目标点列** (像素坐标如何变化, 特殊值-1表示完成)
  - **多视点示例** (不同俯仰角下的目标点差异)

- A.4: 视频数据样本
  - 视频元数据 (512×512, 30FPS, 46帧等)
  - 视频内容对应关系 (每帧对应的导航步骤)

#### 2. 新增附录B: 转换后的训练样本

**内容覆盖:**

- B.1: 单个训练样本的完整结构
  - 原始导航任务 (JSON格式)
  - 转换第一步: 数据加载 (添加data_path和video路径)
  - 转换第二步: 视频处理 (具体的采样计算过程)
    - 详细展示采样计算: 46帧 → 1.533秒 → round(1.533/4)=0 → clip(0,4,8)=4帧
    - 采样帧索引: [0, 15, 31, 45]
    - 视频帧采样时间轴示意图
  - 转换第三步: 文本处理与对话构建
    - 动作到符号的转换 (1→↑, 2→←, 3→→, 0→⊗)
    - 多轮对话格式
  - 转换第四步: Token化与标签生成
    - 详细的Token化演示过程
    - **关键: IGNORE_INDEX=-100的详细说明**
    - 系统/用户消息的labels全为-100 (不计算损失)
    - 助手响应的labels为真实token ID (计算损失)

- B.2: 视觉特征处理
  - 视频到视觉Token的4步转换
  - **详细展示token网格** [4, 26, 26] = 2704个token
  - **时间跨度** second_per_grid_ts的含义

- B.3: 最终训练样本的完整结构
  - 返回给模型的完整字典结构
  - 所有字段的类型和形状

#### 3. 新增附录C: 两种格式数据的详细对比

**C.1: 数据维度对比表**
- 时间维度: 46帧完整 vs 4帧采样
- 空间分辨率: 512×512 vs 1664×1664
- 动作表示: int32 vs 文本符号
- 位姿数据: 显式4×4矩阵 vs 隐含于token
- **目标点坐标: 显式像素坐标 vs 转换为文本响应**
- 文本指令: 原始 vs Token编码
- 存储格式: Parquet+MP4 vs PyTorch张量
- 文件大小: 5MB vs 12KB

**C.2: 数据流对应关系**
- 详细的流程图展示完整的数据转换管道
- 分为视频处理分支和文本处理分支
- 最终合并成完整的training_sample

**C.3: 具体数值示例对比**
- Episode 0的完整转换过程
- 原始数据具体例子
- 5个转换步骤的详细数值计算
- 最终样本的具体形状

#### 4. 新增附录D: 多个样本的批处理

- D.1: Batch内多样性
  - 展示批处理中4个样本的特性
  - 每个样本的长度和特点不同

- D.2: GPU内存占用
  - 文本tokens部分: ~128 KB
  - 视觉特征部分: ~1.75 GB
  - 模型激活值: ~2-3 GB
  - 总占用: ~4-5 GB

#### 5. 新增附录E: 关键转换公式与参数

**E.1: 视频采样公式**
- 具体的公式推导
- 实际计算过程演示

**E.2: Token网格计算**
- 像素到patch的转换
- 最终token网格的大小计算

**E.3: 文本长度计算**
- 系统消息、用户消息、助手响应各部分的token数
- 总长度计算和填充策略

#### 6. 新增附录F: 数据转换检查清单

| 检查项 | 原始数据 | 转换后数据 | 验证方法 |
|-------|---------|----------|---------|
| 样本数量 | 104,870 | 104,870 | `len(dataset)` |
| 视频完整性 | 46帧可读 | 4帧可加载 | `__getitem__` |
| 动作有效性 | {0,1,2,3,5} | token ∈ vocab | 检查labels |
| 目标点有效性 | [0,512]² | 隐含于响应 | 检查parquet |
| 指令完整性 | tasks非空 | input_ids非空 | len > 100 |
| Token化正确 | 原始文本 | input_ids+labels | 解码验证 |
| 掩码正确 | - | -100比例60-70% | 统计 |
| GPU可加载 | N/A | 张量可上传 | 无OOM |

---

## 关键信息对比总结

### 目标点坐标的生成方式

#### 原始R2R中的目标点
```
Parquet数据列: goal.125cm_0deg
类型: int32[2] (像素坐标)

数据范围: 
- 有效目标点: [0, 512] × [0, 512] (在512×512图像中的位置)
- 特殊值: [-1, -1] (表示任务完成, STOP)

生成方式:
1. 机器人在3D环境中的位置 (pose数据)
2. 将3D目标位置投影到2D图像平面
3. 得到像素坐标 [x, y]
4. 作为监督信号引导机器人导航

示例序列:
帧0: [256, 384] → 目标在画面中央偏右下
帧1: [264, 390] → 随机器人移动, 目标相对位置略有变化
...
帧44: [512, 512] → 目标接近画面角落 (接近目标位置)
帧45: [-1, -1]  → 到达目标, 应该STOP
```

#### 转换后训练样本中的目标点
```
转换方式: 
- 目标点坐标 → 转换为文本响应中的动作序列
- 从Parquet提取: actions = [1, 1, 1, ..., 0]
- 转换为符号: "↑ ↑ ↑ ... ⊗"
- Token化后作为labels进行监督学习

在训练样本中:
- 位置: 在assistant响应部分的labels中
- 形式: 文本token ID (而非显式坐标)
- 用途: 指导模型学习导航策略

示例:
原始目标点序列: [256,384] → [264,390] → ... → [-1,-1]
对应的动作序列: [1, 1, ...] → [0]
转换后的文本:   "↑ ↑ ... ⊗"
在labels中:     [26855, 26855, ..., STOP_token]
```

### 两种格式的核心区别

| 特性 | 原始R2R | 转换后样本 |
|------|--------|----------|
| **目标表示** | 显式坐标(x,y) | 隐含于动作序列 |
| **导航监督** | 逐帧像素坐标 | 动作符号序列 |
| **时间维度** | 完整46帧 | 采样4帧 |
| **特征形式** | 低维结构化 | 高维token |
| **模型输入** | 不可直接使用 | 可直接输入VLM |
| **损失计算** | N/A | 仅在assistant部分 |

---

## 文档的新增价值

### 对研究者的帮助

1. **理解数据转换**: 看懂原始数据 → 理解转换过程 → 掌握最终格式
2. **调试数据问题**: 通过具体例子快速定位问题
3. **验证数据质量**: 使用检查清单确保转换正确
4. **优化性能**: 了解参数对转换结果的影响

### 对中期报告的帮助

1. **详实的数据描述**: 可直接用于报告的数据部分
2. **清晰的流程展示**: 丰富的图表和表格
3. **具体的数值计算**: 展示精确的转换细节
4. **多维度对比**: 从多个角度说明原始vs转换后数据的区别

### 对工程实现的帮助

1. **参数配置**: 明确了各个转换参数的含义和影响
2. **错误排查**: 通过具体例子快速定位错误源头
3. **性能优化**: 理解内存占用和计算开销
4. **可复现性**: 详细的公式和计算过程

---

## 文档统计

| 项目 | 数值 |
|------|------|
| 新增内容 | 730行 (从678行→1408行) |
| 新增附录 | 6个 (A-F) |
| 新增小节 | 20个+ |
| 新增表格 | 15个+ |
| 新增代码示例 | 20个+ |
| 新增图表 | 5个 |

---

## 使用建议

### 对中期报告的引用方式

```markdown
## 数据转换过程

### 原始数据格式
参考: DATA_TRANSFORMATION_GUIDE.md 附录A
展示R2R数据的具体结构和样本

### 转换流程
参考: DATA_TRANSFORMATION_GUIDE.md 附录B
详细展示4个转换步骤的完整过程

### 数据对比
参考: DATA_TRANSFORMATION_GUIDE.md 附录C
多维度对比原始vs转换后的数据

### 计算细节
参考: DATA_TRANSFORMATION_GUIDE.md 附录E
展示具体的公式和计算过程
```

### 作为补充材料

该文档可作为:
1. 论文的技术补充材料
2. 代码库的详细文档
3. 新成员的入门教材
4. 复现实验的参考指南

---

## 后续可能的增强

1. **可视化**: 添加实际的视频帧截图和目标点坐标可视化
2. **性能分析**: 添加不同参数配置下的转换速度对比
3. **错误处理**: 添加常见错误及其解决方案
4. **高级话题**: 添加多GPU并行转换、缓存策略等内容

